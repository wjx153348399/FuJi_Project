# 阻抗文件批量上传工具 — 开发前待补充清单

> 请补充以下缺失项后，即可开始开发。

## 一、接口定义

### 1.1 请求规范
- [x] 请求 URL（完整）：`http://192.168.3.207:8443/service-qms/file/importImpedanceData`
- [x] 请求方式：POST
- [x] Content-Type：`multipart/form-data`
- [x] 请求参数名（文件对应的字段名）：`file`
- [x] 单次请求传一个文件还是支持批量？单个和批量都支持具体看文件数量（根据截图，目前按单文件 `file` 字段上传设计）
- [x] 除了文件外还需要传哪些参数？请列出：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| serialNo | string | 是 | 序列号（需确认来源，暂定为空字符串或按需生成） |
| (未命名) | string | 是 | 截图中的第三个参数（需确认具体参数名，暂略） |

### 1.2 请求示例
- [x] 请提供一个完整的请求示例（Headers + Body）：

```http
POST /service-qms/file/importImpedanceData HTTP/1.1
Host: 192.168.3.207:8443
Content-Type: multipart/form-data; boundary=----WebKitFormBoundary7MA4YWxkTrZu0gW

------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="file"; filename="G-LXD-4002-00 F26-308-8059 板内-图形_阻抗.xlsx"
Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet

(文件二进制数据)
------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="serialNo"

(序列号数据)
------WebKitFormBoundary7MA4YWxkTrZu0gW--
```

### 1.3 响应格式
- [x] 请提供成功响应示例：

```json
{
  "code": 200,
  "success": true,
  "message": "操作成功",
  "data": null
}
```

- [x] 请提供失败响应示例：

```json
{
  "code": 500,
  "success": false,
  "message": "文件解析失败",
  "data": null
}
```

- [x] 如何判定上传成功？根据 JSON 返回中的 `code=200` 或 `success=true` 判定。

### 1.4 认证方式
- [x] 接口调用时如何鉴权？
  - [x] 先登录获取 Cookie / Session，再调用接口
- [x] 登录接口地址：待确认（暂定为需要模拟网页登录流程）
- [x] 账号：`dzn`，密码：`asd123,.`

### 1.5 文件路径上传
- [x] "文件路径"是指：
  - [x] B：客户端先读取文件内容，通过 multipart 等方式传给接口

## 二、文件扫描

### 2.1 扫描范围
- [ ] 只扫描以下 4 个子文件夹的当前层级，还是递归扫描更深目录？  到时候会有新的文件夹加进来，后续需要添加新的文件夹时，可以补充。
  - `CP-阻抗\LXD & AFX & JXN`
  - `CP-阻抗\OUTER`
  - `图形后(半成品)\LXD & AFX`
  - `图形后(半成品)\OUTER`

### 2.2 Excel 文件类型
- [ ] 支持的后缀（可多选）：
  - [ ] `.xlsx`
  - [ ] `.xls`
  - [ ] `.csv`
- [ ] 是否需要排除临时文件（如 `~$xxx.xlsx`）？是
- [ ] 是否需要排除非 Excel 文件？是

### 2.3 时间筛选
- [ ] "当天"以哪个时区为准？
  - [ ] 北京时间（UTC+8）

## 三、去重规则

### 3.1 判重方式
- [x] 如何判定"同一文件"？（可多选）
  - [x] 完整路径 + 文件名 + 修改时间

### 3.2 去重记录
- [x] 去重记录存在哪里？
  - [x] 本地 JSON / txt 文件，路径：`\\10.0.8.252\ProductionFieldFile 现场公共盘\阻抗\LOG\uploaded_records.json`
- [x] 去重记录保留多久？长期保留，或者定期清理

## 四、重试机制

### 4.1 重试规则
- [x] 最大重试次数：`2` 次
- [x] 每次重试间隔：`3` 秒（暂定）
- [x] 哪些错误类型需要重试？
  - [x] 只要是错误就重试（网络错误、超时、接口报错等所有异常）

## 五、日志规范

### 5.1 日志文件
- [ ] 日志文件命名规则：
  - [ ] 按天：`YYYY-MM-DD.json`
  - [ ] 按次执行：`YYYY-MM-DD_HH-MM-SS.json`
  - [ ] 其他：__________
- [ ] 日志格式：
  - [ ] 单个 JSON 数组，所有记录在一个文件中
  - [ ] JSON Lines，每行一条记录（推荐）
  - [ ] 其他：__________

### 5.2 日志字段
- [ ] 每条日志记录需要包含哪些字段（请勾选）：

| 字段 | 需要 | 说明 |
|------|------|------|
| 文件名 | ✅ | |
| 上传时间 | ✅ | |
| 上传结果 | ✅ | 成功 / 失败 |
| 上传信息 | ✅ | 错误原因或成功信息 |
| 源目录（子文件夹） | ✅ | 文件来自哪个子文件夹 |
| 完整文件路径 | ✅ | |
| 文件大小 | ✅ | |
| 文件修改时间 | ✅ | |
| 重试次数 | ✅ | |
| 错误类型 | ✅ | |
| HTTP 状态码 | ✅ | |
| 接口响应内容 | ✅ | |
| 接口响应时间(ms) | ✅ | |

## 六、异常处理与通知

### 6.1 异常场景补充
- [ ] 共享盘无法连接 → 报错 + 人工排查
- [ ] 接口超时 → 报错 + 人工排查
- [ ] 文件被占用 → 报错 + 人工排查
- [ ] 文件正在写入（大小变化中）→ 如何处理？__________
- [ ] 上传到一半脚本被中断 → 如何处理？__________

### 6.2 失败通知方式
- [ ] 出现异常时如何通知人工？
  - [ ] 控制台输出
  - [ ] 邮件通知
  - [ ] 企业微信 / 钉钉 / 飞书
  - [ ] Windows 事件日志
  - [ ] 仅记录到日志文件，不做额外通知
  - [ ] 其他：__________
- [ ] 如果选邮件/消息通知，请提供通知配置（收件人、SMTP、Webhook 等）__________

## 七、运行环境与配置

### 7.1 运行环境
- [ ] 服务器操作系统：__________（如 Windows Server 2019）
- [ ] Python 版本：__________（如 3.9+）
- [ ] 脚本运行账号是否有 `\\10.0.8.252` 共享盘访问权限？__________
- [ ] 需要安装的 Python 第三方库：__________（如 `requests`, `openpyxl`）

### 7.2 配置文件
- [x] 共享盘账号密码是否从配置文件读取而非硬编码？
  - [x] 是，配置文件路径：`config.json`（与脚本同目录）
- [x] 接口地址是否可配置化？是，放入 `config.json`



### 7.4 手动触发
- [ ] 手动触发方式：
  - [ ] 双击运行 `.bat` / `.py`
  - [ ] 命令行 `python main.py`
  - [ ] 命令行支持传参（如指定日期）：`python main.py 2026-06-08`


## 八、其他

- [ ] 是否需要上传进度显示（如 5/20 个文件）？是
- [ ] 上传完成后是否需要生成汇总报告？ 列出那些成功那些失败，为什么失败
- [ ] 上传后是否需要移动或删除源文件？不需要，只执行上传文件的这个动作

---

> 请将补充后的结果更新到本文档，完成后告知即可开始开发。
> 创建时间: 2026-06-08
