[OPEN] upload-timeout

# 现象

- 同一个文件首次上传成功
- 再次上传同一文件时，登录成功，但上传接口在 30 秒内无响应，触发 `Read timed out`

# 当前假设

1. 后端对重复导入同一文件会进入更慢的校验/覆盖逻辑，30 秒超时不足。
2. 上传的文件体积较大，后端解析耗时波动明显，首次成功不代表重复上传也能在 30 秒内完成。
3. 网关 `10.0.8.237:9528` 对该请求存在偶发等待或排队，导致脚本侧先超时。
4. 重复文件在服务端触发了特殊分支（查重、回滚、覆盖、锁等待），最终没有及时返回。
5. 当前脚本把“请求完成超时”和“业务失败”都归为失败，但实际服务端可能仍在后台处理。

# 已有证据

- 登录链路正常。
- 测试脚本使用伪造 Excel 时，接口能快速返回业务错误，说明链路本身通。
- 主脚本首次真实上传成功，说明认证、上传地址、form-data 字段基本正确。
- 当前失败模式不是 401/403，而是 `Read timed out`。

# 下一步

- 先基于日志判断最可能原因。
- 如需进一步确认，再增加最小化观测日志或延长超时时间进行验证。
